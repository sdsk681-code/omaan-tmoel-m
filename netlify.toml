[build]
  base    = "."
  command = "pnpm --filter @workspace/mockup-sandbox... run build"
  publish = "artifacts/mockup-sandbox/dist"

# SPA catch-all — all paths serve index.html so React router handles them
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
